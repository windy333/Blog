<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>建站记录</title>
    <url>/post/60687.html</url>
    <content><![CDATA[<h1>分享</h1>
<p>这个网站建立于2024/3，初衷是觉得好玩，作为半个计算机专业的学生也应该有一个属于自己的网站（不过好像建晚了些，不过没关系）这应该算是一个树洞吧，在空闲的时候我会来这里看看，或者记录一些乱七八糟的东西。最后还是蛮喜欢这句座右铭的“纸上得来终觉浅，绝知此事要躬行”，最后的最后就让这篇文章在这里提醒自己吧。</p>
<p>同时感谢Hexo和Butterfly的开发者免费开源</p>
]]></content>
      <categories>
        <category>随笔</category>
      </categories>
      <tags>
        <tag>随笔</tag>
      </tags>
  </entry>
  <entry>
    <title>conda常用命令</title>
    <url>/post/60688.html</url>
    <content><![CDATA[<h2 id="1-前言">1. 前言</h2>
<p>Conda是Anaconda中一个强大的包和环境管理工具，可以在Windows的Anaconda Prompt命令行使用，也可以在macOS或者Linux系统的终端窗口(terminal window)的命令行使用。这里简单介绍conda的一些常用命令（对于大多数人来说掌握了这些就基本上能够‘生活自理’了吧）。本文根据 <a href="https://docs.conda.io/projects/conda/en/latest/user-guide/getting-started.html">conda-getting-started</a>编译而成，喜欢阅读英文的伙伴们可以直接去阅读英文说明。</p>
<h2 id="2-管理conda自身">2. 管理conda自身</h2>
<h3 id="2-1-信息查询">2.1 信息查询</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">conda --version         #查看conda版本</span><br><span class="line">conda config --show     #查看conda的环境配置</span><br><span class="line">conda create --help    #查询某个命令的帮助</span><br></pre></td></tr></table></figure>
<h3 id="2-2-设置镜像">2.2 设置镜像</h3>
<p>conda有时候安装软件会非常慢。设置国内镜像的话可以使安装更快捷一些。设置方法如下所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">#设置清华镜像</span><br><span class="line">conda config --add channels https:<span class="comment">//mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/free/</span></span><br><span class="line">conda config --add channels https:<span class="comment">//mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main/</span></span><br><span class="line">conda config --add channels https:<span class="comment">//mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/conda-forge/</span></span><br><span class="line">conda config --add channels https:<span class="comment">//mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/bioconda/</span></span><br><span class="line">#设置bioconda</span><br><span class="line">conda config --add channels bioconda</span><br><span class="line">conda config --add channels conda-forge</span><br><span class="line">#设置搜索时显示通道地址</span><br><span class="line">conda config --set show_channel_urls yes</span><br></pre></td></tr></table></figure>
<h3 id="2-4-更新conda">2.4 更新conda</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">conda update conda     #将conda自身更新到最新版本</span><br><span class="line">conda update Anaconda  #整个Anaconda都更新到确保稳定性和兼容性的最新版本</span><br></pre></td></tr></table></figure>
<h2 id="3-管理环境">3. 管理环境</h2>
<p>Conda允许你创建相互隔离的独立环境，这些环境被称之为虚拟环境（Virtual Environment），这些环境各自包含属于自己的文件、包以及他们的依存关系，并且不会相互干扰。</p>
<p>Anaconda有一个缺省的名为base的环境。但是不建议把程序放在base环境中，应该创建不同的虚拟环境分别管理不同的开发项目。</p>
<p>这个涉及到一个根本的问题：为什么我们需要虚拟环境呢？举一个简单的例子，想象一下你有多个项目要开发，每个项目中都有一些包要依赖于某个共同的包，但是各自的所需要的版本不一致，有一些需要低版本的，有些需要高版本的，为不同的项目创建虚拟环境就可以把不同项目隔离开来，各自使用自己所需要的软件环境。</p>
<h3 id="3-1-创建虚拟环境">3.1 创建虚拟环境</h3>
<p>使用conda创建虚拟环境的命令格式为，这表示创建python版本为3.8、名字为env_name的虚拟环境。创建后，env_name文件可以在Anaconda安装目录envs文件下找到。在不指定python版本时，自动创建基于最新python版本的虚拟环境.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">conda create -n env_name python=<span class="number">3.8</span></span><br><span class="line">conda create -n env_name numpy matplotlib python=<span class="number">3.8</span>  #创建虚拟环境的同时安装必要的包(并不建议这样做)</span><br></pre></td></tr></table></figure>
<h3 id="3-2-查看有哪些虚拟环境">3.2 查看有哪些虚拟环境</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">conda env list</span><br><span class="line">conda info -e</span><br><span class="line">conda info --envs</span><br></pre></td></tr></table></figure>
<h3 id="3-4-虚拟环境开关">3.4 虚拟环境开关</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">conda activate env_name  #激活</span><br><span class="line">conda activate           #退出</span><br><span class="line">conda deactivate         #退出</span><br></pre></td></tr></table></figure>
<h3 id="3-5-删除虚拟环境">3.5 删除虚拟环境</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">conda remove --name env_name --all           #将该指定虚拟环境及其中所安装的包都删除</span><br><span class="line">conda remove --name env_name  package_name   #只删除虚拟环境中的某个或者某些包</span><br></pre></td></tr></table></figure>
<h3 id="3-6-导出环境">3.6 导出环境</h3>
<p>很多的软件依赖特定的环境，我们可以导出环境，这样方便自己在需要时恢复环境，也可以提供给别人用于创建完全相同的环境。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">conda env export --name myenv &gt; myenv.yml   #获得环境中的所有配置</span><br><span class="line">conda env create -f  myenv.yml              #重新还原环境</span><br></pre></td></tr></table></figure>
<h2 id="4-包（Package）的管理">4. 包（Package）的管理</h2>
<h3 id="4-1-查询包的安装情况">4.1 查询包的安装情况</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">conda list   #查询看当前环境中安装了哪些包</span><br><span class="line">conda search package_name   #查询当前Anaconda repository中是否有你想要安装的包</span><br><span class="line">conda list pkgname          #用conda list后跟<span class="keyword">package</span>名来查找某个指定的包是否已安装，而且支持*通配模糊查找</span><br></pre></td></tr></table></figure>
<h3 id="4-2-包的安装和更新">4.2 包的安装和更新</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">conda install package_name   #在当前（虚拟）环境中安装一个包</span><br><span class="line">conda install numpy=<span class="number">0.20</span><span class="number">.3</span>   #也可以以以下命令安装某个特定版本的包(以下例为安装<span class="number">0.20</span><span class="number">.3</span>版本的numpy)</span><br><span class="line">conda update numpy           #可以用以下命令将某个包更新到它的最新版本</span><br><span class="line"></span><br><span class="line">#安装包的时候可以指定从哪个channel进行安装，比如说，以下命令表示不是从缺省通道，而是从conda_forge安装某个包</span><br><span class="line">conda install pkg_name -c conda_forge   </span><br></pre></td></tr></table></figure>
<h3 id="4-3-conda卸载包">4.3 conda卸载包</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">conda uninstall package_name  #这样会将依赖于这个包的所有其它包也同时卸载</span><br><span class="line">conda uninstall package_name --force  #如果不想删除依赖其当前要删除的包的其他包(不建议，这样会使得你的环境支离破碎)</span><br></pre></td></tr></table></figure>
<h3 id="4-4-清理anaconda缓存">4.4 清理anaconda缓存</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">conda clean -p      # 删除没有用的包 --packages</span><br><span class="line">conda clean -t      # 删除tar打包 --tarballs</span><br><span class="line">conda clean -y -all # 删除所有的安装包及cache(索引缓存、锁定文件、未使用过的包和tar包)</span><br><span class="line">conda clean -h      #关于清除命令的更详细的说明</span><br></pre></td></tr></table></figure>
<h2 id="5-Python版本的管理">5. Python版本的管理</h2>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">conda install python=<span class="number">3.5</span></span><br><span class="line">python --version   #更新完后可以用以下命令查看变更是否符合预期</span><br><span class="line">conda update python   #将python版本更新到最新版本</span><br></pre></td></tr></table></figure>
<h2 id="6-conda-install-vs-pip-install">6. conda install vs pip install</h2>
<h3 id="6-1-有什么区别？">6.1 有什么区别？</h3>
<ol>
<li>conda可以管理非python包，pip只能管理python包。</li>
<li>conda自己可以用来创建环境，pip不能，需要依赖virtualenv之类的。</li>
<li>conda安装的包是编译好的二进制文件，安装包文件过程中会自动安装依赖包；pip安装的包是wheel或源码，装过程中不会去支持python语言之外的依赖项。</li>
<li>conda安装的包会统一下载到一个目录文件中，当环境B需要下载的包，之前其他环境安装过，就只需要把之间下载的文件复制到环境B中，下载一次多次安装。pip是直接下载到对应环境中。</li>
<li>conda只能在conda管理的环境中使用，例如比如conda所创建的虚环境中使用。pip可以在任何环境中使用，在conda创建的环境 中使用pip命令，需要先安装pip（**conda install pip ），**然后可以 环境A 中使用pip 。conda 安装的包，pip可以卸载，但不能卸载依赖包，pip安装的包，只能用pip卸载。</li>
</ol>
<h3 id="6-2-能否混用？">6.2 能否混用？</h3>
<p>首先，不建议混用。混用容易导致库的依赖关系出现混乱，然后突然哪天环境可能就崩了，安装不了新的包，无法进行conda update之类的。</p>
<h3 id="6-3-安装在哪里？">6.3 安装在哪里？</h3>
<ul>
<li>
<p>conda install xxx：这种方式安装的库都会放在anaconda3/pkgs目录下，这样的好处就是，当在某个环境下已经下载好了某个库，再在另一个环境中还需要这个库时，就可以直接从pkgs目录下将该库复制至新环境而不用重复下载。</p>
</li>
<li>
<p>pip install xxx：分两种情况，一种情况就是当前conda环境的python是conda安装的，和系统的不一样，那么xxx会被安装到anaconda3/envs/current_env/lib/python3.x/site-packages文件夹中，如果当前conda环境用的是系统的python，那么xxx会通常会被安装到~/.local/lib/python3.x/site-packages文件夹中 </p>
</li>
</ul>
<h3 id="6-4-如何判断conda中某个包是通过conda还是pip安装的？">6.4 如何判断conda中某个包是通过conda还是pip安装的？</h3>
<p>执行​conda list,用pip安装的包显示的build项目为pypi。</p>
<h2 id="7-conda-configuration">7. conda configuration</h2>
<p>conda的配置文件为&quot;.condarc&quot;，该文件在安装时不是缺省存在的。但是当你第一次运行conda config命令时它就被自动创建了。&quot;.condarc&quot;配置文件遵循简单的YAML语法。可执行conda info查看文件在哪。</p>
<h3 id="7-1-Channel管理">7.1 Channel管理</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">conda config --add channels conda-forge     #追加conda-forge channel</span><br><span class="line">conda config --remove channels conda-forge  #移除conda-forge channel</span><br><span class="line">conda config --get&amp;nbsp;channels            #查询当前配置中包含哪些channels</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>操作技巧</category>
      </categories>
      <tags>
        <tag>-计算机 -conda</tag>
      </tags>
  </entry>
  <entry>
    <title>git操作</title>
    <url>/post/60689.html</url>
    <content><![CDATA[<h2 id="1-一些git常用命令">1.一些git常用命令</h2>
<h3 id="1-上传代码">1.上传代码</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">git push -u &lt;远程仓库别名&gt;   <span class="comment">// 第一次上传加 -u</span></span><br><span class="line">git push &lt;远程仓库别名&gt;      <span class="comment">// 后续上传</span></span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">本地同步线上代码</span><br><span class="line"><span class="comment">// origin 本地仓库别名</span></span><br><span class="line"><span class="comment">// master 本地仓库分支名</span></span><br><span class="line">git pull origin master</span><br></pre></td></tr></table></figure>
<h3 id="2-分支">2.分支</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">git branch   <span class="comment">//获取本地的所有分支</span></span><br><span class="line">git branch &lt;分支名&gt;   <span class="comment">//创建新分支</span></span><br><span class="line">git checkout &lt;分支名称&gt;   <span class="comment">//切换分支</span></span><br></pre></td></tr></table></figure>
<h3 id="3-修改">3.修改</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">git remote remove &lt;远程仓库的别名&gt;   <span class="comment">//删除指定名称的远程仓库</span></span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">git branch -m [&lt;原分支名称&gt;] &lt;新的分支名称&gt;   <span class="comment">//修改分支名称,如果不指定原分支名称则为当前所在分支</span></span><br><span class="line">git branch -M [&lt;原分支名称&gt;] &lt;新的分支名称&gt;   <span class="comment">//强制修改分支名称</span></span><br><span class="line">git branch -d &lt;分支名称&gt;   <span class="comment">//删除指定的本地分支</span></span><br><span class="line">git branch -D &lt;分支名称&gt;   <span class="comment">//强制删除指定的本地分支</span></span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">保留仓库，清空仓库的所有文件</span><br><span class="line">git rm <span class="string">&#x27;*&#x27;</span></span><br><span class="line">git commit -m <span class="string">&#x27;备注&#x27;</span></span><br><span class="line">git push -u origin master</span><br></pre></td></tr></table></figure>
<h3 id="4-其他">4.其他</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">github上的版本发布</span><br><span class="line"></span><br><span class="line">本地版本号</span><br><span class="line">git tag -a v1<span class="number">.0</span><span class="number">.0</span></span><br><span class="line"></span><br><span class="line">把这个版本发布到线上</span><br><span class="line">git push --tags</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">远程仓库名修改，本地需要先删除，重新添加</span><br><span class="line"></span><br><span class="line">查看本地所有仓库</span><br><span class="line">git remote -v</span><br><span class="line"></span><br><span class="line">删除本地仓库</span><br><span class="line">git remote rm &lt;仓库名&gt; </span><br><span class="line"></span><br><span class="line">重新添加</span><br><span class="line">git remote add origin 远程仓库链接</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">遇到问题：</span><br><span class="line"></span><br><span class="line">上传步骤：</span><br><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;提示消息&quot;</span></span><br><span class="line">git push origin master</span><br><span class="line"></span><br><span class="line">出错：</span><br><span class="line">! [rejected] master -&gt; master (fetch first)</span><br><span class="line"> error: failed to push some refs to <span class="string">&#x27; 。。。&#x27;</span></span><br><span class="line"></span><br><span class="line">出现这个问题是因为github中的README.md文件不在本地代码目录中</span><br><span class="line"></span><br><span class="line">解决： git pull --rebase origin master</span><br></pre></td></tr></table></figure>
<h2 id="2-下载git工具，并安装">2.下载git工具，并安装</h2>
<p><a href="https://git-scm.com/downloads">git下载地址</a></p>
<h2 id="3-初始化本地仓库">3.初始化本地仓库</h2>
<p>选择一个文件夹作为本地仓库，输入命令将这个文件夹初始化为 git 仓库</p>
<p>注：存在一个隐藏文件夹，命令执行成功之后没有出现的话，文件夹左上角 - 查看 - 勾选隐藏的项目</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">git init</span><br></pre></td></tr></table></figure>
<h2 id="4-设置用户名、邮箱">4.设置用户名、邮箱</h2>
<p>设置用户名和邮箱</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">git config --global user.name <span class="string">&quot;Your Name&quot;</span></span><br><span class="line">git config --global user.email <span class="string">&quot;email@example.com&quot;</span></span><br><span class="line">git config user.name     #查看设置的用户名</span><br><span class="line">git config user.email    #查看设置的邮箱</span><br></pre></td></tr></table></figure>
<h2 id="5-生成SSH-Key密钥">5.生成SSH Key密钥</h2>
<p>由于本地Git仓库和GitHub仓库之间的传输是通过SSH加密的，所以必须要让github仓库认证你SSH key，这里输入前面设置的邮箱</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t rsa -C <span class="string">&quot;youremail@example.com&quot;</span></span><br></pre></td></tr></table></figure>
<p>连续按下三次回车（存在密码需要输入密码）</p>
<p>密钥一般存放在C盘 - 用户 - 用户名下的 .ssh 文件夹（如下图）</p>
<h2 id="6-获取SSH-Key密钥">6.获取SSH Key密钥</h2>
<p>输入如下命令，可以直接在git终端获取 id_rsa.pub 文件里面的密钥</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">cat  ~/.ssh/id_rsa.pub</span><br></pre></td></tr></table></figure>
<p>选中全部密钥（ssh-rsa开始，一直到.com结束），右键 Copy，把全部密钥复制下来</p>
<blockquote>
<p>如果用命令获取密钥失败，直接去存放密钥的文件夹找到 id_rsa.pub 文件，用记事本打开，将里面的全部内容复制下来</p>
</blockquote>
<h2 id="7-配置github密钥">7.配置github密钥</h2>
<ol>
<li>打开 github - settings (设置)</li>
<li>选择 SSH and GPG keys</li>
<li>点击 New SSH key (新建 ssh 密钥)</li>
<li>title是备注，将复制的密钥填入，然后点击 Add SSH key (新增 ssh 密钥)</li>
<li>创建github仓库</li>
<li>复制远程仓库的ssh地址</li>
</ol>
<h2 id="8-本地仓库链接远程仓库">8.本地仓库链接远程仓库</h2>
<p>注：检查仓库是否添加成功</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">git remote add 仓库别名 仓库地址</span><br><span class="line">git remote -v  #查看仓库别名</span><br></pre></td></tr></table></figure>
<h2 id="9-测试链接是否成功">9.测试链接是否成功</h2>
<p>输入命令测试线路是否连通</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ssh -T git<span class="meta">@github</span>.com</span><br></pre></td></tr></table></figure>
<p>若果出现如下，继续阅读，否则跳过</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">你好，xxxxx！您已成功通过身份验证，但GitHub不提供shell访问。</span><br><span class="line">Hi xxxxx! You<span class="string">&#x27;ve successfully authenticated, but GitHub does not provide shell access.</span></span><br></pre></td></tr></table></figure>
<p>输入命令 <strong>ssh-keyscan -t rsa <a href="http://github.com">github.com</a> &gt;&gt; ~/.ssh/known_hosts</strong></p>
<p>解决方法： <a href="http://www.imooc.com/wenda/detail/589935">Git错误：连接到远程存储库时“主机密钥验证失败”</a></p>
<h2 id="10-本地代码提交至远程仓库">10. 本地代码提交至远程仓库</h2>
<p>第一次提交需加上 <strong>-u</strong>，第二次开始不再需要,<strong>origin</strong> 是仓库别名，<strong>master</strong> 提交远程仓库的分支名</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&#x27;本次提交的备注&#x27;</span></span><br><span class="line">git push -u origin master</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>操作技巧</category>
      </categories>
      <tags>
        <tag>-计算机 -git -github</tag>
      </tags>
  </entry>
</search>
